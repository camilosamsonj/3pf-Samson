import { FontSizeModifyDirective } from './font-size-modify.directive';

describe('FontSizeModifyDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeModifyDirective();
    expect(directive).toBeTruthy();
  });
});
